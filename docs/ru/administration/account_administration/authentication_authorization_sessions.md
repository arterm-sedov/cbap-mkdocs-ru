---
title: 'Аутентификация, авторизация и сеансы пользователей'
kbId: 4656
tags:
tags:
    - Active Directory
    - Basic
    - SSO
    - авторизация
    - аутентификация
    - безопасность
    - интеграция
    - интеграция с внешними системами
    - протоколы
    - сеансы пользователей
    - сессии
    - токен
hide: tags
---

# Аутентификация, авторизация и сеансы пользователей {: #authentication_authorization_sessions }

Система поддерживает следующие способы аутентификации:

- **Basic** — внешняя система должна передать имя пользователя и пароль в заголовке HTTP-запроса при обращении по API к **{{ productName }}**.
- **HMAC** — внешняя система должна передать токен и секретный ключ в заголовке HTTP-запроса при обращении по API к **{{ productName }}**.
- **Аутентификация через форму** — при работе в браузере **{{ productName }}** отображает начальную страницу с полями для ввода имени пользователя и пароля.
- **Через SSO** — используется механизм единого входа (Single Sign-On), при котором учетные данные пользователя автоматически передаются из операционной системы для аутентификации без необходимости ручного ввода.

После успешной аутентификации для пользователя создаётся сеанс (и только один).

- Сеанс завершается, если пользователь неактивен в течение 30 минут.
- При обновлении страницы счётчик времени активности пользователя обнуляется.
- Максимальный срок жизни сеанса составляет 15 дней, после чего потребуется заново войти в **{{ productName }}**.
- После повторного входа в **{{ productName }}** создаётся новый сеанс.

    _![Диаграмма аутентификации и управления пользовательскими сеансами](https://kb.comindware.ru/assets/img_63bc194ca9eb4.png)_

В **{{ productName }}** также реализованы механизмы аутентификации при интеграциях с внешними системами:
{: .pageBreakBefore }

- Active Directory;
- SMTP;
- IMAP;
- Microsoft Exchange;
- веб-сервисы, поддерживающие протокол OData;
- ADFS.

<div class="relatedTopics" markdown="block">

--8<-- "related_topics_heading.md"

- [Самостоятельная регистрация и вход через сервер каталогов. Настройка][registration_and_login]
- [Ключи аутентификации API. Определения, настройка, удаление][authentication_keys]
- [Сервер каталогов. Настройка подключения][ad_connection]
- [Linux. Аутентификация через Active Directory. Настройка контроллера домена и экземпляра ПО][ad_authentication_configure_dc_instance]
- [Linux. Аутентификация через единый вход (SSO). Настройка контроллера домена, экземпляра ПО и компьютера конечного пользователя][sso_authentication_configure]
- [Windows. Настройка единого входа (SSO-аутентификации) в ОС Windows][sso_authentication_configure_windows]

</div>

{% include-markdown ".snippets/hyperlinks_mkdocs_to_kb_map.md" %}
