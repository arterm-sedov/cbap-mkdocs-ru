---
title: 'Атрибут типа «Текст». Настройка цвета отображения значения'
kbId: 5350
url: 'https://kb.comindware.ru/article.php?id=5350'
updated: '2026-06-16 19:15:40'
---

# Атрибут типа «Текст». Настройка цвета отображения значения

Все атрибуты в системе отображаются в определённом формате. Настроить определённый шрифт или размер шрифта для значений конкретного атрибута нельзя. Если же вы хотите, чтобы какой-либо атрибут в таблице окрашивался определённым цветом по заданному условию (например, если задача просрочена, выделить значение красным цветом, в противном случае — зелёным), то это возможно через атрибут типа «**Текст**» с форматом отображения «**HTML-текст**».

## Порядок настройки

1. Создайте атрибут типа «**Текст**», выберите формат его отображения «**HTML-текст**». Отметьте флаг «**Вычисляемый**».
2. В поле «**Вычисляемое выражение**» выберите «**Формула**» и вставьте выражение:

```
FORMAT(
IF(EQUALS($StatusRef->name,"Выполнено"),
"<p><span style='color:#000000'><span style='background-color:#3ee028'>{0}</span></span></p>",
"<p><span style='color:#000000'><span style='background-color:#f02626'>{0}</span></span></p>"),
LIST($StatusRef->name))
```

## Синтаксис

Для решения задачи используется функция `FORMAT()`, которая форматирует значение из нужного атрибута (в данном примере, *«Статус»*). Далее прописывается условие с помощью функции `IF()`. В нашем случае это *«Статус»* равен *«Выполнено»*. После этого, если условие выполняется, то задний фон окрашивается в зелёный цвет, а текст — в чёрный. Если условие не выполняется, то задний фон окрашивается в красный цвет, а текст — в чёрный.

Для окрашивания заднего фона и текста используется HTML — язык гипертекстовой разметки. Цвет заднего фона можно поменять в значении `background-color:#f02626`. Цвет текста можно поменять в значении `color:#000000`.

В конце выражения в `LIST()` прописывается атрибут, значение которого нужно окрасить. В нашем случае это `$StatusRef->name`. Нужно заметить, что окрасится не сам атрибут *«Статус»*, а отдельный вычисляемый атрибут, который вы создали.

Примечание

Ограничений в настройке нет: можно окрашивать только текст, только фон, добавлять дополнительные HTML-значки и т. д.

## Связанные статьи

- [Атрибут типа «Текст»](https://kb.comindware.ru/article.php?id=5710)
- [Цветовой индикатор в таблице. Настройка отображения](https://kb.comindware.ru/article.php?id=5349)
- [Язык формул](https://kb.comindware.ru/category.php?id=901)
- [Справочник цветов HTML](https://colorscheme.ru/html-colors.html)